# Runtime deps for yapper STT/TTS workers.
# Install into .venv (prefer system CUDA torch when using --system-site-packages).
#
# Security-critical ML versions are exact. Chatterbox 0.1.7 is installed from
# its verified sdist by scripts/install_chatterbox.py after replacing only its
# vulnerable torch/torchaudio metadata pins.
openai-whisper==20250625
torch==2.10.0
torchaudio==2.10.0
numpy>=1.24,<2.0; python_version < "3.13"
numpy>=2.0; python_version >= "3.13"
soundfile==0.14.0
